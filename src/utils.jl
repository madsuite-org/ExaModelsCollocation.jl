# Helper functions

# ---------- initialize.jl ----------
# Convert initial guess into ExaModels init form
function _convert_init(init::NamedTuple, dims)

    return init_exa
end

# TODO _has_z, y , u, p, theta
# TODO boolean has checks to quickly determine build paths